# realtime_calls

Reusable foreground audio/video call primitives for Flutter apps.

The package owns the public call API, WebRTC session lifecycle, media controls,
and lightweight call widgets. Host apps provide signaling by implementing
`CallSignaling`.
